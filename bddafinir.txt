CREATE TABLE Articles(
		id INTEGER PRIMARY KEY AUTOINCREMENT,
		name TEXT,
		description TEXT,
		quantity INTEGER,
)

CREATE TABLE Commandes(
		id INTEGER PRIMARY KEY AUTOINCREMENT,
		list_articles LIST
		status TEXT,
		FOREIGN KEY (list_articles) REFERENCES Articles(id)

)
	